{
    "version": "0.2.0",
    "configurations": [
    {
        "type": "firefox",
        "request": "launch",
        "reAttach": true,
        "name": "Launch add-on",
        "addonPath": "${workspaceFolder}"
    },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch LegalScout",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/pages"
        }
    ]
}